import React from 'react';
import logo from './../../asset/logo.jpg';

const Layout = () => {
  return (
    <div style={{ padding: "10px 5px 0 5px", }}>
      <img className="mt-1 ml-3" src={logo} width={100} height={70} />
    </div>
  );
};

export default Layout;
